import { HeroSection9 } from "@/components/ui/hero-section-9";

/**
 * Standalone hero (HeroSection9) for preview.
 * Visit /hero to see it.
 */
export default function HeroPage() {
  return (
    <div className="-mx-4 w-[calc(100%+2rem)] max-w-none sm:-mx-6 sm:w-[calc(100%+3rem)] md:-mx-8 md:w-[calc(100%+4rem)]">
      <HeroSection9 />
    </div>
  );
}
